import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile-game-development',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
